{
  "name": "tad-engine",
  "private": true,
  "description": "Commit-linting config only; this repo's content is Python + docs, not JS.",
  "devDependencies": {
    "@commitlint/cli": "^21.2.2",
    "@commitlint/config-conventional": "^21.2.2"
  }
}
